import { GstFormatDirective } from './gst-format.directive';

describe('GstFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new GstFormatDirective();
    expect(directive).toBeTruthy();
  });
});
