import { OmitSpecialCharactersDirective } from './omit-special-characters.directive';

describe('OmitSpecialCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new OmitSpecialCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
