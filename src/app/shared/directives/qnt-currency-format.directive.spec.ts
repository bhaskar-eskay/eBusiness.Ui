import { QntCurrencyFormatDirective } from './qnt-currency-format.directive';

describe('QntCurrencyFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new QntCurrencyFormatDirective();
    expect(directive).toBeTruthy();
  });
});
