import { AppMatTabDirective } from './app-mat-tab.directive';

describe('AppMatTabDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMatTabDirective();
    expect(directive).toBeTruthy();
  });
});
