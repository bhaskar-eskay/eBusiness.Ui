import { AppMatSelectDirective } from './app-mat-select.directive';

describe('AppMatSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMatSelectDirective();
    expect(directive).toBeTruthy();
  });
});
