import { FocuschangedDirective } from './focuschanged.directive';

describe('FocuschangedDirective', () => {
  it('should create an instance', () => {
    const directive = new FocuschangedDirective();
    expect(directive).toBeTruthy();
  });
});
